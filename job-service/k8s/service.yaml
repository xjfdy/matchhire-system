apiVersion: v1
kind: Service
metadata:
  name: job-service
spec:
  type: NodePort
  selector:
    app: job-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: grpc
      port: 9091
      targetPort: 9091